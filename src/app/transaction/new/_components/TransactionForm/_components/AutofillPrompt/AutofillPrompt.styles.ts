"use client";

import styled from "@emotion/styled";
import { theme } from "@/src/lib/theme";

export const PromptBody = styled.p`
  margin: 0;
  color: ${theme.foreground.secondary};
  font-size: ${theme.fontSize.md};
`;
