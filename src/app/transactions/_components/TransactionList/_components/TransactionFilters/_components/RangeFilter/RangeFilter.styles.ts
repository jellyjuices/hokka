"use client";

import styled from "@emotion/styled";
import { theme } from "@/src/lib/theme";

export const RangeDates = styled.div`
  display: inline-flex;
  align-items: center;
  gap: ${theme.space.xs};
`;
